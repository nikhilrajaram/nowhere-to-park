# === AWS ===
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
S3_BUCKET_NAME=nowhere-to-park
S3_REGION=us-east-1

# === WORKER ===
ALLOWED_ORIGIN=http://localhost:5173
# object key of the tileset within the bucket; also defaulted in worker/wrangler.toml [vars]
PMTILE_FILENAME=parking.pmtiles
